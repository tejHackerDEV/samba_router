name: samba_router
description: An router for Samba-Server which is meant to build for speed .
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  samba_helpers:
    git:
      url: https://github.com/tejHackerDEV/samba_helpers
      ref: main

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
